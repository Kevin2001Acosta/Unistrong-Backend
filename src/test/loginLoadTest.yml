config:
  target: "http://localhost:3001"
  phases:
    - duration: 120
      arrivalRate: 500
scenarios:
  - flow:
      - post:
          url: "/user/login"
          json:
            email: "cliente@gmail.com"
            password: "contraseña12A*"
