config:
  target: "http://localhost:3001"
  phases:
    - duration: 120
      arrivalRate: 20
scenarios:
  - flow:
      - post:
          url: "/user/login"
          json:
            email: "testuser@example.com"
            password: "password123"
