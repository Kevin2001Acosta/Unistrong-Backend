config:
  target: "http://localhost:3001"
  phases:
    - duration: 60
      arrivalRate: 10
scenarios:
  - flow:
      - post:
          url: "/user/login"
          json:
            email: "cliente@gmail.com"
            password: "contraseña12A*"
